#include <stdio.h>

/**
 * main - print the name of the file the program was compiled
 * from, followed by a new line.
 *
 * Return: 0(Success)
 */
int main(void)
{
printf("%s\n", __FILE__);
return (0);
}
