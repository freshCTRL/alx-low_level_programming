#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point.
 * @argc: argument to main.
 * @argv: argument to main.
 *
 * Return: 0(Success)
 */
int main(int argc, char *argv[])
{
printf("%d\n", argc);
return (0);
}
