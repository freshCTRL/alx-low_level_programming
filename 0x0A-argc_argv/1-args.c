#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point.
 * @argc: argument to main.
 * @argv: argument to main.
 *
 * Return: 0(Success)
 */
int main(int argc, char __attribute__((unused)) *argv[])
{

{
printf("%d\n", argc);
}
return (0);
}
